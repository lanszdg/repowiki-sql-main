CREATE TABLE INV_TXN (
  ID NUMBER PRIMARY KEY,
  ITEM_ID NUMBER NOT NULL,
  QTY NUMBER,
  STATUS VARCHAR2(8)
);

CREATE TABLE INV_AUDIT (
  ITEM_ID NUMBER,
  ACTION VARCHAR2(32)
);

CREATE SEQUENCE INV_TXN_SEQ;
