CREATE OR REPLACE PACKAGE INVENTORY_PKG AS
  PROCEDURE bulk_receive(p_item_id IN NUMBER, p_qty IN NUMBER, p_result OUT VARCHAR2);
  FUNCTION get_stock(p_item_id IN NUMBER) RETURN NUMBER;
END INVENTORY_PKG;
/
